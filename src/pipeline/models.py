"""Shared pipeline data models.

Models that are specific to a pipeline step (e.g., Pretalx, YouTube, text generation)
should be defined in their respective submodules instead of here.
"""

# This file is intentionally left mostly empty as models have been moved
# to their respective submodules:
# - Pretalx models: pipeline.pretalx.models
# - YouTube models: pipeline.youtube.models
# - Text generation models: pipeline.text_generation.models
